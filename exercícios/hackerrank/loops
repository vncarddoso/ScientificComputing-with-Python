# Task: O código fornecido lê um número inteiro, n, da entrada padrão (STDIN). Para todos os números inteiros não negativos i < n, imprima i^2.
# Exemplo: n = 3, A lista de números inteiros não negativos menores que n = 3 é [0, 1, 2]. Imprima o quadrado de cada número em uma linha separada.

if __name__ == '__main__':
    n = int(input("Digite um número: "))
    for i in range(n):
        raiz_quadrada = i * i
        print(raiz_quadrada)